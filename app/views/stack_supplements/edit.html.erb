
<h3>Editing <%= @stack_supplement.supplement.name %> in <%= @stack_supplement.stack.name %> </h3>
  

<%= form_for [@stack, @stack_supplement] do |f| %>
  <% if @stack_supplement.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(@stack_supplement.errors.count, "error") %> prohibited this stack_supplement from being saved:</h2>
      <ul>
      <% @stack_supplement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :dose %><br>
    <%= f.text_field :dose, class: 'form-control', placeholder: "Enter Dose" %>
  </div>

   <div class="form-group">
    <%= f.label :frequency_id %><br>
    <%= f.text_field :frequency_id, class: 'form-control', placeholder: "Enter Frequency" %>
  </div>
 <div class="form-group">
  <%= f.collection_select :frequency_id, Frequency.all, :id, :value, include_blank: false %>
  </div>

  
  <div class="form-group">
    <%= f.submit  class: 'btn btn-success' %>
  </div>
<% end %>


<%= link_to 'Show', stack_stack_supplement_path(@stack, @stack_supplement) %> |
<%= link_to 'Back', stack_stack_supplements_path(@stack) %>
