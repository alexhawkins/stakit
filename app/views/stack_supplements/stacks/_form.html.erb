<%= form_for (@new_stack)  do |f| %>
  <% if @new_stack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_stack.errors.count, "error") %> prohibited this stack from being saved:</h2>

      <ul>
      <% @new_stack.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="input-group stack-supp-input-group">
  <span class="input-group-addon"><i class="glyphicon glyphicon-plus"></i></span>
  <div class="form-group main-stack">
    <%= f.text_field :name, class: 'form-control main-stack-control', placeholder: 'What\'s your stack\'s name?' %>
  </div>
</div>
  <div class="form-group">
    <%= f.submit "Create Stack", class: 'btn btn-green pull-right' %>
  </div>
<% end %>
