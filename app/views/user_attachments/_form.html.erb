<%= form_for(@user_attachment) do |f| %>
  <% if @user_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_attachment.errors.count, "error") %> prohibited this user_attachment from being saved:</h2>

      <ul>
      <% @user_attachment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
   <div class="form-group">
    <%= f.submit "Upload Image", class: 'btn btn-primary' %>
  </div>
<% end %>